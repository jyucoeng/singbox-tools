# 回源 / 多协议 Argo 改造计划（V4：含 128MB 可安装性保证与旧版兼容）

> 状态：**待用户确认**（未开始动代码）
> 关联：
> - `sb00.sh` 为开发主线（与 `sb.sh` 同源，改动需同步两份）
> - 辅助界面 `singbox-tools-interface`（`index.html` 唯一源，`worker.js` 由 `build.py` 生成）
>
> 硬约束：
> 1. 一期、二期都要支撑 **128MB NAT 机器**（本版给出"装得下"的保证策略）
> 2. 架构对"未来新增协议"可延展（表驱动）
> 3. **旧版兼容**（v4 补全）

---

## 0. 核心模型：双通道共存

```
                    ┌──────────────────────────────────────────────┐
  ① Argo 隧道通道     │  同一个 nginx（统一 HTTP 入口，双监听）       │
  cloudflared ───────►│   listen 127.0.0.1:${argo_pt} / ${nginx_pt} │
                    │                                              │
  ② Origin rules ────►│  /${uuid}-x → sing-box ws 端口（按协议）     │
                    └──────────────────────────────────────────────┘
```

- 两通道只是"两个前门"，**并存零新增进程**（nginx/sing-box/配置不变，只多输出一套节点 + 一条 CF 规则提示）。
- 变量模型：`argo`（ws 协议多选 / all）+ `backhaul`（argo 缺省 / origin / both）+ `origin_domain`（单值即可，多协议靠 nginx path 聚合，**不随协议数增长**）。

### 0.1 ⚠️ NAT 机端口可达性（很重要）

- **Argo 通道**：出站连接，**零入站端口**，NAT 机无条件可用（这是 NAT 机的保底）。
- **Origin 通道**：需要 CF 能连回 VPS，NAT 机必须命中**服务商公网端口映射**。
  - `nginx_pt` 默认 8080 **在 NAT 机上大概率没有公网映射**（8080/80/443 常被面板占用或不在映射范围）；
  - 修正：`nginx_pt` 本就是可配项，**NAT 机用户必须把它设为服务商映射范围内的公网端口**；
  - CF Origin Rule 的目标在 NAT 机上 = **服务商共享公网 IP + 该映射端口**（服务商公网 IP 由 icanhazip 检测可得）；
  - 服务商不给任何入站映射 → origin 通道不可用，只走 Argo。
- `origin_domain` 单值即可；即便 NAT 机多协议，也只用 1 个域名（nginx path 聚合）。

---

## 1. 可延展性架构：ws 回源协议注册表（表驱动）

```bash
# key|label|singbox_type|transport|path|port_file|flag
WS_CDN_PROTOS=(
    "vmess|Vmess|vmess|ws|vm|port_vm_ws|vmp"
    "trojan|Trojan|trojan|ws|tr|port_tr|trp"
    "vless|Vless|vless|ws|vl|port_vl_ws|vlp"
)
```

配套访问器 `_ws_pick/_ws_label/_ws_type/_ws_transport/_ws_path/_ws_portfile/_ws_flag/_ws_link`。
把原"每协议 7 处硬编码"收敛为 4 处循环（激活 / 入站生成 / nginx location / 节点链接）。

**未来加协议 = 注册表加 1 行 + 加 1 个 `_ws_link_<key>` 函数（~10 行）+ 若新增 transport 则 nginx/sing-box 加 1 个分支。**
**interface 同步：表单由 JS 配置数组 (`WS_PROTOS_UI`/`DIRECT_PROTOS_UI`) 渲染，加协议只加 JS 一行。**

---

## 2. 128MB 可安装性保证（本版重点）

### 2.1 第一性：内存不由"复杂度"决定，由"进程数 + 系统开销"决定

本方案**不新增进程**，sing-box 永远 1 进程（多入站在内部）、cloudflared 1 个、nginx 1 个 → 进程数与今天完全一致。复杂度不会让内存爆炸。

### 2.2 内存预算（典型值 / 上界，Alpine 基准；Ubuntu 系统项另列）

| 组件 | 常驻 | 备注 |
|---|---|---|
| sing-box（3ws + 多直连入站） | 10-30 MB | 每 ws 入站 +0.5-2MB，可忽略 |
| cloudflared（QUIC 默认） | 30-50 MB | **尖峰 60-80**，最大压力源 |
| cloudflared（`--protocol http2`） | 25-40 MB | 强烈建议 128MB 使用 |
| nginx（worker_processes 1） | 5-15 MB | |
| 系统（Alpine + OpenRC） | ~10 MB | |
| 系统（Ubuntu + systemd + journald） | 40-60 MB | **128MB 机真杀手** |

### 2.3 分场景验证（128MB）

| 场景 | 全栈（Alpine） | 全栈（Ubuntu） | 128MB 判定 |
|---|---|---|---|
| 现状：单 Argo + 少量直连 | 50-90 | 90-130 | 紧张可行（现状） |
| 多协议 Argo（3 ws 同开） | 55-95 | 95-135 | 可行（Alpine）；Ubuntu 需 swap |
| **双通道并存 + 3ws 全开** | 55-100 | 95-140 | **Alpine+http2+swap 可行；Ubuntu 不建议** |
| 只 origin（无 cloudflared） | 20-40 | 60-100 | 最宽裕 |
| **所有东西全开 + Ubuntu 无 swap** | 130-170 | — | **装不下，禁止** |

### 2.4 保证"装得下"的手段

1. **低内存守卫（一期实现）**：安装时检测 `free`，RAM < 200MB 时：
   - 提示使用 Alpine + swap；
   - cloudflared 自动加 `--protocol http2`（省 10-20MB、压尖峰）；
   - nginx 生成 `worker_processes 1`；
   - 建议"按需选协议"，不推荐全选。
2. **默认不做全开**：`argo=all` 是可选项，128MB 用户按需选 1-2 个 ws。
3. **swap 是标配**：128MB NAT 机普遍配 512MB swap，能吸收 cloudflared 尖峰（代价仅是短期性能下降）。
4. **origin 通道是 128MB 的最优解**：无 cloudflared → 全栈降至 20-40MB。

### 2.5 明确边界

- **保留功能、不砍复杂度**：因为复杂度与内存无关，砍掉只会损失能力、换不来 128MB 安全。
- **若坚决不接受任何守则**（不要 swap、要全开、非 Ubuntu 不可）→ 砍到最小集：**只做"多协议 Argo"单通道**（进程 3、无 origin、改动最小），其余一律不做。

---

## 3. 旧版兼容策略

### 3.1 向后兼容（旧配置/命令/文件跑在新脚本）——目标：强兼容

| 旧货 | 新脚本行为 | 判定 |
|---|---|---|
| 环境变量 `argo=vless`（单值） | 单元素列表，行为完全不变 | ✅ |
| 旧 `vlvm` 单值文件（Vmess/Trojan/Vless） | "包含匹配"，单值正常命中 | ✅ |
| 无 `backhaul`/`origin_domain` 文件（老安装） | 缺省 = `argo` 通道 = 旧行为；节点只出隧道 | ✅ |
| 旧接口生成的无 `backhaul` 命令 | 直接可用 | ✅ |
| 维护命令 list/node/sub/res/del（读文件反推） | 不依赖 env，照常 | ✅ |

### 3.2 向前兼容（新文件被旧脚本读）——退化不崩溃

| 新货 | 旧脚本行为 | 判定 |
|---|---|---|
| `vlvm` 逗号列表（Vmess,Vless） | 旧脚本精确匹配 `= "Vmess"` 失败 → argo 节点不输出 / need_argo=false，**不崩溃但能力退化** | ⚠️ 明确"不建议回退脚本版本" |
| 新落盘 `backhaul_file`/`origin_domain_file` | 旧脚本不读，忽略 | ✅ |

### 3.3 升级迁移（重要，不自动，需 rep）

- **脚本升级不会自动改已运行配置**。旧配置继续按旧方式跑；执行 `sb rep <新参数>` 才启用新功能。
- 升级后的维护命令（list/node/sub）在旧单值 `vlvm` 上立即可用，无需 rep。
- `sb rep` 语义沿用现状：**不带 argo/backhaul 参数会清掉 ws 协议配置**（旧版已如此，非新引入）；计划在 README 明确"重装请带上你要的 argo/backhaul"。

### 3.4 存量异常文件的处理

- **老单 vless 曾复用 port_vm_ws 端口**：若 port_vl_ws 不存在、vim 与 vlp 同时启用时不复用（修 bug 的修复点）；若已存在同值端口文件且两协议同时启用 → 自动重随机，避免 sing-box 起不来。
- **残留的 port_vm_ws 文件（无对应入站）**：rep 会被 cleandel 清理，不存在长期污染。

---

## 4. 一期实现清单（合并）

### 4.1 sb00.sh

| # | 改点 |
|---|---|
| 1 | `WS_CDN_PROTOS` 注册表 + 全部访问器 |
| 2 | `_argo_proto_list()` + `ARGO_PROTOS`（支持列表/all，兼容单值） |
| 3 | 激活/校验改表驱动 |
| 4 | vless 端口复用 bug 修复 |
| 5 | `vlvm` 列表化（"包含匹配"读取，新老兼容） |
| 6 | `backhaul` + `origin_domain` 解析/校验/落盘（缺省 argo） |
| 7 | `ensure_cloudflared_if_needed` → 按 backhaul 是否含 argo 判定；http2 参数 + 低内存守卫 |
| 8 | `ensure_nginx_if_needed` → backhaul 含 origin 也装 nginx；worker_processes 1（低内存） |
| 9 | 节点输出双通道（argo@argo_domain / origin@origin_domain + CF 提示） |
| 10 | 交互菜单：协议多选 + 通道选择 + origin 域名 |
| 11 | **NAT 端口可达性引导（一期落地）**：检测 NAT 机；origin 通道启用时提示「nginx_pt 需设为服务商映射范围内的公网端口」；节点/CF 提示里的 origin 回源地址 = 服务商公网 IP + 实际配置端口 |
| 12 | 文案/注释/help/showmode/README |
| 13 | 版本号递增 |

### 4.2 singbox-tools-interface

| # | 改点 |
|---|---|
| 1 | 协议表单 JS 表驱动（`WS_PROTOS_UI`/`DIRECT_PROTOS_UI`） |
| 2 | 「回源方式」选择（Argo/回源/两者）→ `backhaul` |
| 3 | `origin_domain` 输入框（origin/both 显示） |
| 4 | `buildSb()` 组装 argo/backhaul/origin_domain |
| 5 | 联动：backhaul=origin 隐藏 agn/agk |
| 6 | origin 模式下 NAT 引导文案：「请把 Nginx 订阅端口(nginx_pt) 设为服务商映射端口」+ CF Origin Rule 指向示例 |
| 7 | 文案更新 + `python build.py` |

---

## 5. 二期（合并）

| # | 改点 |
|---|---|
| 1 | backhaul=origin 完整流程（不装/停 cloudflared、可选关 8001） |
| 2 | NAT 端口映射**自动自检**（一期仅提示，二期可主动检测可达性并给出诊断） |
| 3 | 低内存硬化落地验证 + README 128MB 建议 |
| 4 | 更多协议扩展的验证性回归 |

---

## 6. 发布与同步策略（已确认）

- **只改 `sb00.sh`**：全部改造（含各期）只在 `sb00.sh` 上进行；**`sb.sh` 是线上正式环境，本项目不动它**。
- **同步由用户手动执行**：等 `sb00.sh` 全部改造完成并经实际验证后，由用户自己把 `sb00.sh` 复制覆盖到 `sb.sh`。
- **interface 的发布时序联动**：`singbox-tools-interface` 的命令 URL 指向 `sb.sh`（`BASE + 'sb.sh'`）。所以 interface 做完 `backhaul/origin_domain` 新字段后，生成的新参数在 `sb.sh` 未同步上线前**不生效**。发布顺序应为：
  1. `sb00.sh` 改造完成 + 验证；
  2. 用户复制 `sb00.sh` → `sb.sh`；
  3. interface 上线（或随验证后一起发布）。
  - 若要加速联调，可在过渡期让 interface 暂指 `sb00.sh`（BASE 一行），正式发布前改回 `sb.sh`。

## 7. 决策待确认项

1. **保留双通道功能 + 低内存守卫**（推荐，128MB 有保障）还是 **砍到最小集"只多协议 Argo"**？
2. 表驱动重构一次性做（推荐）还是先补丁？
3. interface 的 JS 表驱动一起做，还是界面只先加 backhaul/origin_domain 表单？
4. 变量命名 `argo` + `backhaul` + `origin_domain` 确认？
5. 你提到"4 期"——当前计划是「一期（多协议+双通道框架）+ 二期（origin 深度/NAT/128MB）」，请对齐你心中的 4 期划分（是否有明确的分期清单，我一并排进计划）。